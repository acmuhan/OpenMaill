services:
  web:
    image: ghcr.io/acmuhan/openmaill:latest
    pull_policy: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_DEBUG: "0"
      OPENMAIL_HTTP_TIMEOUT: "${OPENMAIL_HTTP_TIMEOUT:-30}"
    restart: unless-stopped
